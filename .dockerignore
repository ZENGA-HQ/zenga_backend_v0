# Ignore dependencies and build outputs
node_modules
dist

# Ignore local env files
.env
.env.*

# Ignore git and editor folders
.git
.vscode
.idea

# Misc
npm-debug.log*
server_output.txt
Dockerfile
docker-compose.yml


# # 1. Build and start the containers in the background
# docker compose up --build -d

# 2. View the logs to ensure everything is running
# docker compose logs -f app
